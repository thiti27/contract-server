// Placeholder — this project does not use passport today. /api/login
// (app/controllers/authController.js) checks credentials directly against
// app_users/admin_users with a single raw query, no passport strategy involved.
// Kept as an empty stub so the project's standard config/ layout is complete; wire up
// a real strategy here if/when this app adopts passport-based auth. Nothing currently
// imports this file.
export default {};
